# Import Race Discovery

> How to discover not-yet-imported JTU/IRONMAN editions for /import-race (deterministic, no fragile SPA scraping)

Discovery recipe used by `/import-race` (see also [jtu-results-api](jtu-results-api.md), [runnet-results-api](runnet-results-api.md)):

**Inventory:** editions in race-info.json key off `edition.date` (NOT a `year` field). JTU dirs = `master/<year>/<event_id>/` (e.g. kanagawa); IRONMAN dirs = `master/<year>/<event_id>_<year>/`. Don't diff by source_url event_id alone — editions imported via the JSON API / runnet lack `event_id=` in source_url and show as false-missing. Cross-check against actual master/ dirs.

**JTU discovery:** scan the events API across the year's event_id range — `https://results.jtu.or.jp/api/events/search?cond%5Bevent_id%5D=NNN` → **`json.res.body`**`.events[0]` (event_name, event_date) + **`json.res.body`**`.programs[NNN][]`. The payload is wrapped in a top-level `res` key: reading `json.body` yields `undefined` for every id, which looks exactly like "no new races" (cost a full sweep pass on 2026-09-13 — assert a nonzero count on a known-populated id before trusting any sweep). Filter date year, exclude already-imported, then drop non-age-group programs (学生/大学/U23/ジュニア/キッズ/高校/パラ/デュアスロン/アクアスロン/リレー/ワールド・アジアカップ elite). **国スポ and 日本選手権 ARE in scope** (user decision 2026-09-13) — the repo already carries `kokuspo_shiga` 2025 and `sprint_championship_aomori` 2025; they were previously on this exclusion list by mistake. 2025 ids ≈ 307–377, 2026 ≈ 378–426 (06-21 weekend added 398 gamagori-orange, 399 fukuyama, 400 banpaku/吹田226). Per-program columns vary by year (a race can be run-bike-run one year, swim-bike-run the next) — build each edition's mapping from the ACTUAL captions, don't clone blindly. Some result_tables are broken (real header sits in the first data row, e.g. tangram 308_2). caption→role table: 総合順位=overall_rank, No.=bib, 氏名=name, 年齢=age, 性別=gender, 居住地/登録地/都道府県=residence, 総合記録=total_time, PNLT=penalty, 男子順位/女子順位/男女別順=gender_rank, 年齢区分/区分=age_category, 年齢別順/年齡別順/区分順位=age_rank; segments: スイムラップ=swim lap, S順=rank, T1=swim transition, バイクラップ=bike lap, B順=rank, スプリット=bike cumulative_time, 通過=bike cumulative_rank, T2=bike transition, ランラップ=run lap, R順=rank. JTU editions carry a `weather_file`.

**IRONMAN discovery:** scrape each event page `https://labs-v2.competitor.com/results/event/<event_uuid>` → extract `__NEXT_DATA__` → `props.pageProps.subevents[]` (wtc_eventdate, wtc_eventid=subevent_uuid, wtc_name); keep year-matching subevents with date ≤ today (already raced). Collect event_uuids from scripts/im703-all-subevents.json + im703-event-uuids.json + ironman-subevent-ids*.json (the *-all-subevents snapshots are STALE for the current year — use them only for uuids, not for which editions exist). **Build the scan map fresh** (e.g. `/tmp/im-uuids.json` = {event_uuid: base}) from im703-event-uuids.json (62) + ironman-subevent-ids.json (~30 full-IRONMAN, value.event_uuid+.name) → ~92 events. **CAVEAT: this map is INCOMPLETE** — events not in those files are invisible to the scan (a 06-17 run missed im703_eagleman + ironman_austria 2026 because the then-current map lacked their uuids; a wider rebuild caught them 2 days later). When a run finds "nothing new," remember it only covered the ~92 mapped events. Also: labs-v2 returns non-200 to `curl -I` HEAD — use GET. Then fetch results: `https://labs-v2.competitor.com/api/results?wtc_eventid=<subevent_uuid>` → resultsJson.value[] — `scripts/fetch-ironman-results.js --input <file>` does this (needs the race-info 2026 edition to already exist so it resolves the tsv path). IRONMAN editions have NO weather_file. Match scan names to dir bases carefully: Muscat=im703_oman, Penghu=ironman_taiwan, Goseong=im703_goseong→im703_goseong_korea, Frankfurt=ironman_frankfurt, and beware token collisions (Western Massachusetts ≠ western_sydney). **PARTIAL-RESULTS GOTCHA:** a race imported the day after it's held often returns only the PRO field (e.g. Frankfurt 2026 fetched 58 MPRO vs 2498 in 2025 — AG results not yet uploaded). After fetching, sanity-check the athlete count against the prior year and the division breakdown (all `MPRO`/`FPRO` = incomplete); if it's pro-only, ROLL BACK and defer to a later run rather than commit partial data. A subevent often has a single uuid that fills in AG athletes a day or two later.

**Non-JTU PDF races** (木更津/kisarazu, 宮崎シーガイア, etc.): not in the JTU API — fetch the result PDF from the official site (kisarazu = `kisarazu-tri.com`, homepage links `result/results_kisarazu{year}.pdf`; note the PDF may carry stale prior-year pages at the end). Kisarazu PDFs glue bib+surname into one token like the old teganuma ones — split leading digits. **Discipline-cancelled / run-only editions:** model as `distance: "OTHER"` with only the held segment(s); segment `columns` can be empty (`minItems:0`, precedent: takamatsu/fujihill) and `total_time` carries the time; estimate the segment distance from finish times and SAY SO in the description (precedent: osaka 2020 swim-cancelled — "実距離は公式公表されておらず…推計…近似値のため参考扱い"). 木更津 2026 was run-only (~7km est.).

**race-info edits:** the file is byte-identical to `json.dumps(data, indent=2, ensure_ascii=False)+"\n"`, so load→mutate→dump is safe; verify the change is purely additive (event count unchanged, only target editions +1, 0 existing editions mutated) — line-diff looks huge when prepending into newest-first events but that's just git realignment. Images are event-level, so new editions of existing events need no new image. Known repo gap: `normalize-tsv` fails output-schema validation for every edition due to `age_category_raw` not being in result-schema.json (see `sport-enum-schema-gap`) — not an import error.

**2026-07-21 run learnings:**
- **Duplicate-event trap:** before creating a "new" event, ALWAYS `grep '"id": "<candidate>"'` the full events array — a race can exist with only pre-2025 editions (im703_gdynia had 2015–2024 editions; a fresh event entry was wrongly created and had to be merged; the existing images/im703_gdynia.webp got clobbered and needed `git show HEAD:<path> >` restore). check:duplicates does NOT catch duplicate event ids.
- **Returning-race blind spot:** races that skipped a year vanish from the last-year-edition scan (gdynia skipped 2025, returned 2026-07-19; les_sables_dolonne_vendee skipped 2025, returned 2026-07-05). Candidate query: IRONMAN events whose latest edition ≤ prior year; get their event uuid from `https://www.ironman.com/races/<slug>/results` → the labs-v2 iframe src (works with plain curl+UA; ~half the slugs 403 on repeat hits — retry with sleep, or WebSearch/coachcox `series/NNN` page for edition-year lists).
- Discontinued/no-2026: im703_st_george ended with 2025 ("final lap" stgeorgeutah.com); im703_campeche & im703_marbella no 2026 edition; IRONMAN France Nice 2026 absent from labs-v2 subevents (listed on pre-season calendars but no results event); marbella 2025 = the 70.3 Worlds (already im703_wc_*).
- scripts/ironman-2026-batch6-subevents.json now carries event_uuid for lake_placid/ecuador/luxembourg/gdynia/les_sables etc. → future map builds include them.
- Swim-cancelled detection: emptySwim==100% of finishers in the fetched TSV (ohio 2026, luxembourg 2026) → model as bike-run (drop swim segment, keep MD/LD, description "Swim was cancelled..."; precedent im703_victoria/im703_dallas 2026). Integrity then reports 0 regressions instead of thousands of missingLaps.
- JTU 2026 quirks: hiwasa switched to per-gender tables + a 総合 table WITHOUT a 性別 column — synthesize 性別 by bib-join against the 男子/女子 tables (412_1/412_2); captions differ per year (oigawa 2026 uses half-width kana ﾍﾟﾅﾙﾃｨ/ｽｲﾑﾗｯﾌﾟ). 'Skip' status token must be uppercased to SKIP (tsv-lint NON_VALUE is case-sensitive).

**2026-08-03 run learnings** (last import was 2026-07-27; this run covered the ~1-week gap):
- **IRONMAN discovery — the sitemap method supersedes the batch-uuid-file method.** `https://www.ironman.com/sitemap.xml?page=1..5` yields every race slug (~169 of them); `/races/<slug>/results` embeds the labs-v2 `/results/event/<uuid>` link. This finds event_uuids for races NEVER in any of the accumulated `scripts/ironman-*subevents*.json` batch files — a plain sweep of only the known-uuid set had a blind spot of 78 uncovered imported events; the sitemap sweep cut that to 4 (all outside the checked date window anyway). The full slug→uuid map from the 2026-08-03 run is saved at `scripts/ironman-site-event-uuids.json` — read this FIRST in future runs instead of reconstructing from the batch files.
- **Partial swim-cancellation (harder case than the full-cancellation precedent):** IRONMAN 70.3 Boise 2026 had swim cancelled for AG athletes only (wind/chop) while PRO did a modified swim minutes later — a genuine 2-wave subevent split in the labs-v2 data (separate subevent_uuid per wave, zero contactid overlap, fetched+merged manually). Detection signal differs from the " 100% empty swim" full-cancellation case: check emptySwim rate by division (MPRO/FPRO vs AG), not just overall — here it was ~94% (1258/1334), not literally 100%. Modeled the same way as full cancellation regardless (drop the swim segment repo-wide for the category, note in description that PRO retains swim splits in the raw TSV column that just isn't mapped) rather than splitting into two categories — keeps one TSV per edition, consistent with how every other IRONMAN edition in this repo is structured.
- **JTU relay/leg filtering by column, not by program:** numadu 2026 (event_id 417) merged an individual race and a 3-person relay leg into ONE program/result_table (417_1) with 322 rows, distinguished only by a `部門名` column (エイジ vs 駅伝1区) — the usual "exclude by program_name" filter doesn't catch this since it's one program. Always check for a division/tag column within a table before assuming program-level filtering is sufficient.
- New precedent: [duathlon-import-policy](duathlon-import-policy.md) — standalone duathlon events (not just aquathlon) are importable with user confirmation.

**2026-08-13 run learnings** (last import 2026-08-03; covered a ~10-day gap):
- **JTU "already imported" checks: don't trust a bare event_id= grep in source_url.** Many JTU editions in this repo carry a blank `source_url` (empty string) — e.g. tangram_madarao, miyazaki_seagaia, takamatsu, hiwasa, murakami, fujikawaguchiko, kokuspo_shiga, sprint_championship_aomori, fukuyama, kisosangawa 2025 editions all have `source_url: ""`. A diff that only extracts `event_id=NNN` from source_url will wrongly flag these as missing. Cross-check candidates by **edition date** against the repo (same JTU event_id ⇒ same race date, and a repo event already having an edition on that exact date is strong evidence it's imported) before treating a "no event_id in source_url" hit as a real gap.
- **Exact-date matching alone is too loose to declare "0 missing" outright** — many unrelated races share a weekend, so "some repo edition exists on this date" doesn't prove it's *this* race. Use it only to shortlist candidates, then confirm each by name.
- Scanned JTU event_id 300–428 this run (417 was the prior ceiling; 418–428 all empty ⇒ confirmed current ceiling still 417, no new ids in the gap).
- **IRONMAN labs-v2 event page redirect gotcha:** fetching `/results/event/<uuid>` sometimes returns a **one-line body** that is itself just another path (`/results/event/<other-uuid>`) instead of the full page — i.e. the subevent_uuid you have isn't the *canonical* event uuid. Follow that redirect (fetch the other-uuid URL) to get the real page with `__NEXT_DATA__.props.pageProps.subevents[]`. Symptom: response body length ~50 bytes, no `__NEXT_DATA__` found. Hit this on IRONMAN Canada-Ottawa (subevent uuid `bf442555-...` redirected to canonical event uuid `b8e35e3d-...`).
- Found and imported: **2026 IRONMAN Canada-Ottawa** (2026-08-02, full distance, 2482 finishers, 0 missing-lap/total regressions). `ironman_ottawa` event DOES carry a `weather_file` (overseas IRONMAN full-distance races in this repo get weather too, per [weather-data-policy](weather-data-policy.md) — the earlier "IRONMAN editions have NO weather_file" note was specific to the 70.3 batch-import editions, not a universal rule; check the sibling edition's `weather_file` field before assuming either way).
- Re-confirmed dormant status (correctly NOT imported): im703_rwanda (last raced 2024, no 2025/2026 subevent), im703_nelson_mandela_bay (last raced 2024, no 2025/2026 subevent). Re-confirmed `im703_subic_bay_philippines` is a stale duplicate of the current `im703_subic_bay` event (same `duplicate-event-trap` pattern as gdynia) — not a real gap, just old cruft worth merging someday.
- **Background discovery agents may go idle without ever delivering a final report message** (SendMessage requests got no reply, and the agents vanished from ListAgents after a multi-day gap/session compaction) — but their intermediate work products (raw API responses, computed JSON diffs) often survive in the scratchpad directory and are directly reusable. Check the scratchpad for leftover files (e.g. `jtu/e*.json`, `missing.json`, `matches.json`, `sitemap*.xml`) before re-running an expensive scan from scratch.

**2026-08-24 run learnings** (last import 2026-08-13; covered an 11-day gap):
- **JTU event_ids run chronologically by race date** — confirmed by sweeping ids 378–414 (the range below the prior run's 417 ceiling) for gap-window dates and finding none; all fell before 2026-07-26. Next run: it's safe to start the id sweep at (prior ceiling − ~5) rather than re-walking the full lower range, as long as the prior run's ceiling was itself confirmed (ids past it scanned empty).
- New ceiling 420 (was 417). Found 2 new events, both first editions, both raced 2026-08-23: **つがる縄文トライアスロン大会 (Tsugaru Jomon Triathlon)**, event_id 418, Shariki/Tsugaru-shi Aomori, OD (1.5/40/10) — official site jomontri.com; **うつくしまトライアスロンinいわき (Utsukushima Triathlon in Iwaki)**, event_id 420, Onahama/Iwaki-shi Fukushima, OD — distinct from the existing `utsukushima_aizu` event (different city). New JTU caption seen: **選手権順 → role `championship_rank`** (NCS age-group championship-series selection rank; build-athlete.js has this case but stores nothing — same no-op pattern as `note`). Also saw **東北順位/福島順位 (regional rank, broader than a host-city "citizen" rank) → mapped to `note`**, not `citizen_rank`/`citizen_category` (those are reserved for host-city-resident rankings per the 柏市民/我孫子市民 precedent).
- **IRONMAN discovery coverage gap is real and worth naming when reporting**: of ~114 events needing a "still current?" check, only 70 had a slug in `scripts/ironman-site-event-uuids.json`; 44 were skipped (mostly long-dormant/discontinued candidates). This pool recurs every run since the uuid map isn't regenerated — a full sitemap re-crawl would close it but wasn't judged worth the cost for a routine gap-fill run.
- **Auto-import cron stall detected**: no `chore: daily auto-import` commit since 2026-07-21 (a month, vs. the expected daily cadence) — surfaced to the user as a flag, not silently absorbed into the manual import. Two of this run's IRONMAN finds (Rio 2026-08-09, Venice-Jesolo 2026-05-03) predate even the prior manual run (08-13), meaning the backlog had been silently growing. Check `~/bin` launchd scripts / dirty-tree-skip logic per `daily-auto-import` if this recurs.
- **Swim-cancellation, another instance**: IRONMAN Leeds 2026 — Waterloo Lake blue-green algae forced a full swim cancellation (confirmed via news search, not just the 100%-empty-Swim-column heuristic); modeled as bike-run only (dropped the swim segment from race-info.json, kept distance LD), consistent with the im703_victoria/im703_dallas 2026 precedent. Detection: `awk` a full-column non-empty count across the whole TSV, not just a few sample rows — this one was literally 0/1224 non-empty, not merely "mostly empty."
- **New duplicate-event-id instance found and fixed**: `im703_venice_jesolo` (single 2026-05-03 edition, source=coachcox.co.uk, committed months ago in "feat: add May 2026 races") was a stale duplicate of the long-standing `im703_venice` (6 editions back to 2021, the correct canonical id) — caught only by `check:duplicates` (98.8% row overlap) AFTER adding the 2026 edition to `im703_venice` via the normal discovery pipeline, not by a pre-check (the event existed under `im703_venice_jesolo`, a plausible-looking distinct id, so a bare `grep '"id": "im703_venice"'` alone wouldn't have caught it — check for name/location matches across *all* event ids sharing key location tokens, not just the exact target id). Fix: kept the edition on the canonical event, reused the duplicate's already-validated `weather-data.json` (saved a regeneration), deleted the duplicate event/TSV/weather/image entirely. This was flagged to the user via AskUserQuestion as scope-expanding before finalizing (approved). `im703_subic_bay_philippines` (see 2026-08-13 note) remains the only other known duplicate-event-id case in the repo, still unmerged.
- Both new JTU TSVs needed a manual full-width→half-width name fix pass after the initial API pull (full-width Latin name "ＳＩＭＳ　ＤＡＶＩＤ"→"SIMS DAVID", full-width space between family/given name, and one double half-width-space case) — the JTU API does not pre-normalize this; always run the tsv-lint name checks on freshly-built TSVs before considering the import done, not just at the final `bun run test` gate.

**2026-08-31 run learnings** (last import 2026-08-24; covered a 7-day gap):
- **A "NEW event" claim from a discovery agent must be verified by date+location against race-info.json BEFORE presenting it to the user, not after.** This run's IRONMAN agent reported two "brand-new events" — "IRONMAN Japan South Hokkaido" (2025-09-14) and "IRONMAN Emilia Romagna" (2025-09-20) — both turned out to be exact-date matches already sitting under existing ids (`ironman_japan` and `ironman_italy`/Cervia respectively). A one-line `node -e` date lookup against the claimed event id before drafting the confirmation list caught both; had they gone to the user unverified, importing them would have created two more venice_jesolo-class duplicates. **Lesson: any "no existing event" claim from a discovery agent gets a cheap disconfirming check (search race-info.json by name AND by the claimed date) before it's trusted, symmetric with how "missing" claims already get cross-checked.**
- **`check:duplicates` is a load-bearing gate, not a formality — it caught 2 more pre-existing duplicate-event pairs this run, in the opposite direction from the above.** Adding editions to `im703_brasilia` (2025-04-13) and `im703_greece` (2025-10-26) — both legitimate-looking "next edition of an existing id" adds — surfaced that `im703_brazil` (richer history, 2006-2015+2025) and `im703_marathon` (2024+2025, and "Marathon" is the literal correct venue name, confirmed by byte-identical athlete/time data) already had that exact date under a *different* event id. Same underlying bug class as venice_jesolo/aix_en_provence (a differently-named duplicate id created at some point in the past for the same physical race), just discovered from the other direction — not "new event that's actually old" but "edition-add that's actually already covered elsewhere." Resolution pattern: drop the redundant fetch, merge the *other* id's remaining edition(s) into the richer/more-specifically-named canonical event, delete the thinner duplicate (event + TSV + weather + image). Always run `check:duplicates` immediately after every edition-add batch, before spending effort on weather/normalization for those specific editions — it's cheap and catches this class reliably where name-based pre-checks can't (the two ids share no common substring: "brasilia"/"brazil" and "marathon"/"greece").
- **Known remaining duplicate-event-id case, not yet fixed**: `im703_subic_bay_philippines` (dup of `im703_subic_bay`) — still just sitting there, low priority since it isn't actively colliding with anything.
- **Consolidating an `integrity-baseline.json` entry after a category-id rename: edit the single key in place (Read + Edit), never `JSON.parse` → mutate → `JSON.stringify(data, null, 2)` the whole file.** `Object.keys()` iteration order differs subtly enough from however the file was originally written (or a prior manual edit left it not-quite-sorted) that a full round-trip rewrite touched ~2600 unrelated lines for a 2-key rename. Same caution applies to any large hand-maintained JSON file in this repo that isn't machine-regenerated by a script (race-info.json IS safe to load→mutate→dump per the earlier note in this memory, because it's always been maintained that way — integrity-baseline.json apparently is not, or drifted).
- New JTU caption seen: **ミニラン** (Yamagata Zao, between S順 and バイクラップ) — a short (~3min, low-variance across athletes, no dedicated rank column) connecting jog from swim exit to bike transition at a venue where the lake and T1 are physically separated (盃湖 → 上の台ゲレンデ). Modeled as `role: "transition"` on the swim segment (T1-equivalent), not as its own `run` segment — the low variance and absent rank column are the signal that it's structural (like a long T1) rather than a competitively-timed leg. If a future race shows a similar "mini run" column WITH its own rank column and higher time variance, treat it as a genuine intermediate run segment instead (duathlon-style, sport:"run" mid-sequence — see the kisosangawa 2025 run-bike-run precedent).
- Deferred by user choice (not imported this run, uuids saved for next time): **IRONMAN 70.3 Zell am See** (2026-08-30, uuid `f40d667a-c78e-4da8-bfeb-a2041c951226`) and **IRONMAN 70.3 Poznan** (2026-08-30, uuid `92f91ff5-f329-4355-8075-f76cdee57f99`) — both raced the day before this run, deferred purely for the 1-day-old-results freshness margin, not a data-quality concern (both already showed healthy full-AG row counts, not the PRO-only gotcha). Import directly next run without re-discovering.
- Auto-import cron still stalled as of this run (no commit since 2026-07-21, now 5+ weeks) — restated across two consecutive runs now, evidence is accumulating that this needs a direct look at `~/Library/Logs/triathlon-daily-import.*` rather than just re-flagging it a third time.

**2026-09-07 run learnings** (last import run 2026-08-31, whose batch was still staged and uncommitted; covered a 7-day gap):
- **This playbook moved into the repository on this run**, because the user reported the import could not be run on another host: `.claude/commands/import-race.md` was already committed, but everything that made it *work* — this discovery file and the source-specific references beside it — lived only in a host-local memory directory keyed by the checkout path. The moved SKILL.md also had a stale step 2 (JTU via the `result_program` SPA + Playwright, the method this file says to avoid); it now points at the JSON API. **Append future run learnings here, not to a host memory.**
- **JTU API response shapes — exact, because guessing them cost a wasted scan pass.** All three endpoints wrap their payload in `res.body`:
  - `/api/events/search?cond%5Bevent_id%5D=NNN` → `res.body.events[0]` (`event_name`, `event_date`, `event_location`, `event_result_opened`) and `res.body.programs["NNN"][]` (`program_id`, `program_name`).
  - `/api/programs/<pid>/result_tables` → `res.body` is a **bare array** of tables (no `result_tables` key); take `[0].result_table_id`.
  - `/api/results?cond%5Bresult_table_id%5D=NNNN` → `res.body.result_cols[]` (caption in `result_col_caption`, order in `result_col_order`) and `res.body.result_list[]` (**`result_list`**, not `results`; cells are `col_1`..`col_N` positional, `null` = empty).
  A wrong key guess yields a silent zero-row / zero-event result, which looks exactly like "nothing new" — always assert a nonzero count on a range you know is populated before trusting a sweep.
- New JTU ceiling **424** (was 420); 425–450 scanned empty. Found 422 伊良湖 / 423 富士河口湖 / 424 佐渡, all raced 2026-09-06.
- **New category: fujikawaguchiko 個人ハーフスイム** (423_2, 15 athletes) — the OD course with only the swim halved to 0.75km. Confirmed against the official entry page (mtfuji-tri.jp/entry/), which also gives the 2026 bike leg as **41km** where 2025 was recorded as 40km. Imported as its own category `fujikawaguchiko_halfswim`, distance `OD` (total 51.75km); it is an individual entry, not a relay. Its table has no 区分/区分順位 columns.
- Relay exclusions this run were program-level and clean: 423_3 リレー, 424_3 RAタイプ, 424_4 RBタイプ (the RA/RB tables are identifiable by their SWIM氏名/BIKE氏名/RUN氏名 columns).
- **Weather generation is now two committed scripts** instead of throwaway scratchpad code — `scripts/gen-weather-jma.py` (domestic) and `scripts/gen-weather-open-meteo.py` (overseas). Both were validated by **regenerating an already-committed file and diffing**, which is how the undocumented conventions were recovered:
  - Overseas: `pressure_msl` (not `surface_pressure`) and `wind_speed_unit=ms`; with those, the generator reproduces `master/2026/im703_vichy_2026/weather-data.json` field-for-field.
  - Domestic: JMA `hourly_s1`, **現地気圧** (cell index 1, not the sea-level column), and `minTemp`/`maxTemp` from the **monthly `daily_s1` page** (cells 7/8) — the hourly page only samples on the hour, so its extremes are not the official daily ones. Reproduces `master/2025/fujikawaguchiko/weather-data.json` on every observed field.
  - Residual, deliberate differences from older files: wind speed keeps JMA's 0.1 m/s precision (older files rounded to integers) and 不快指数 uses the canonical 0.81T + 0.01H(0.99T−14.3) + 46.3 (older files vary by ±1–2, and are not self-consistent under any single formula). Sunrise/sunset are computed astronomically and land within ~1 minute of the older values.
- **JMA station ids used** (prec_no/block_no): 伊良湖 **51/47653** (47654 is not a station — an easy off-by-one), 河口湖 49/47640, 相川 54/47602, 新潟 54/47604, 名古屋 51/47636.
- **Pre-existing weather data-quality findings, not introduced by this run:** `master/2025/sado/weather-data.json` was generated from **新潟 (mainland, ~60km across the sea)**, not 相川 on Sado itself — the two differ materially (mainland max 32.2°C vs island 30.0°C on 2025-09-07). This run used 相川 for sado 2026, so that series is now inconsistent until 2025 (and earlier) are backfilled. `master/2025/irago/weather-data.json` matches no JMA station tested and records an impossible sunset of 19:28 on 2025-09-14 (~18:02 is correct at that latitude) — it should be regenerated.
- **IRONMAN:** the sitemap-derived `scripts/ironman-site-event-uuids.json` (169 events) scanned cleanly with 0 errors over a 2026-08-01..09-07 window. Found **IRONMAN 70.3 Baku (2026-09-05) — a genuinely new event**, inaugural: its canonical labs-v2 event page lists exactly one subevent. Verified against race-info.json by name and by `azerbaijan|baku|caspian` token search (0 hits) before creating the event, per the duplicate-event rule above. The redirect gotcha appeared again (the subevent uuid returns a ~50-byte body containing the canonical event path).
- **`pro=0` is not by itself a partial-upload signal.** im703_knokke_heist 2026 returned 0 PRO rows where 2025 had 46, but its age-group field was *larger* than 2025 (2260 vs 1954 across 21 age groups) — a race simply not hosting a pro field that year. The PRO-only gotcha is the opposite shape: pro count ≈ total row count.
- **Source-side missing splits, baselined:** im703_baku 2026 has 19 finishers with a blank Bike or Run split but a valid total, and fujikawaguchiko 2026 has 3 finishers with blank bike+run laps but a valid 総合記録 — verified in the raw source rows, so recorded via `check-integrity.js --update` rather than treated as an import defect.

- **JMA publishes the 天気 (weather symbol) column late — a freshly imported domestic race gets an all-`UNKNOWN` weather file if you don't handle it.** Found only by an aggregate scan (`weatherCode` distribution across all 2,227 weather files): all three JP editions imported this run were 100% UNKNOWN, while the 2025 file used to validate the generator was fine. The 天気 cell is simply empty on `hourly_s1` for recent dates, and some automated stations (相川) never report it at all. `scripts/gen-weather-jma.py` now fills **only the empty condition cells** from Open-Meteo's WMO code for the same hour and coordinates (`--no-fill-weather` opts out); every measured quantity still comes from JMA, and a date where JMA does have 天気 is left untouched (verified: the 河口湖 2025 output is byte-identical before and after the change). Cross-checked the filled codes against JMA's own 降水量 column — 伊良湖 25mm from 12h, 河口湖 73mm from 9h, 相川 0mm all day — and the filled RAIN/CLEAR pattern matches in all three.
- **Never judge a generated weather file by one edition.** The single-file spot check that validated the generator looked perfect; the defect was visible only in the corpus-wide `weatherCode` histogram. Same lesson as the aggregate-audit rule for TSVs.
- **`master/2025/irago/weather-data.json` was broken and is now regenerated** (it recorded an impossible sunset of 19:28 for 2025-09-14 at 34.6°N — the true value is 18:02 — and temperatures matching no JMA station). Regenerated from 伊良湖 51/47653. **A spot check of `master/2024/irago/weather-data.json` shows the same signature** (stored 3am 27.3°C/70% vs JMA 25.8°C/91%, sunset 18:06 vs 18:11), so the irago series is very likely mis-sourced for 2019/2022/2023/2024 as well — regenerating them is a pending user decision, not done here.
- Sado station question resolved by measurement rather than preference: 相川 (54/47602) is ~6 km from the 佐和田 swim start, so it is used for 2026; `master/2025/sado/weather-data.json` came from 新潟 (54/47604), 60 km away on the mainland. Open-Meteo ERA5 at the exact venue coordinates was fetched as an independent cross-check and agrees with 相川 to ~1–2°C, which is what settled it — the remaining 相川 gap was the missing 天気 column, now filled from that same source rather than by switching the whole file to a model reanalysis (which would have thrown away real visibility and station observations).
- Repo-wide daylight-length scans need latitude awareness: a naive "8h–16h daylight" filter flags ~150 legitimate high-latitude European summer editions (Frankfurt, Elsinore, Jönköping, Haugesund, Finland). Useless as written; only the physically impossible cases (a Japanese September sunset at 19:28) are worth chasing.

**2026-09-13 run learnings** (last import run 2026-09-07, committed; covered a 6-day gap):
- **The JTU JSON path is `json.res.body`, not `json.body`.** My first sweep of ids 419–452 reported *every id empty* — including 420/422/423/424, which the 09-07 section of this file records as populated. That contradiction is the only reason the bug was caught. The 09-07 note already warned about exactly this failure mode ("a wrong key guess yields a silent zero-row result, which looks exactly like nothing new"); the paragraph above is now explicit. **Always sanity-assert against an id this file says is populated before concluding "nothing new."**
- New JTU ceiling **426** (was 424); 427–452 scanned empty. 425 青の煌めきあおもり国スポ2026 and 426 オールキッズトライアスロンジャパン, both raced 2026-09-13.
- **国スポ / 日本選手権 are in scope** — see the corrected exclusion list above. This was surfaced by noticing `kokuspo_shiga` (2025-09-28, 成年男子/成年女子, 91+83 rows) already in the repo while the filter said to drop such races. When a filter rule and the repo's contents disagree, the repo wins — check before skipping.
- **DEFERRED, ready to import next run — JTU 425 青の煌めきあおもり国スポ2026 (2026-09-13, 青森市特設トライアスロン会場, OD).** Do not re-discover; everything needed is here:
  - programs `425_1` 成年女子 → result_table **4609** (81 rows), `425_2` 成年男子 → result_table **4627** (91 rows). Data is complete (full splits, DNF/DNS rows present), not a partial upload.
  - **Deferral reason was weather, not results:** JMA had not published the 2026-09-13 hourly table at import time (`gen-weather-jma.py` → "JMA table not found"), while the same station generated 2026-09-12 cleanly. Its 2025 sibling carries a `weather_file`, so importing same-day would have meant a knowingly incomplete edition.
  - JMA station **青森 = prec_no 31 / block_no 47575**, verified by fetching the page and reading the station name off it (31/47574 is 深浦, 47576 むつ, 47581 八戸 — the same off-by-one trap as 伊良湖 47653/47654). Venue coords ≈ 40.8222 / 140.7474.
  - Model on `kokuspo_shiga` 2025 (new event id `kokuspo_aomori` per the host-named precedent; 国スポ rotates prefecture, so each host gets its own event). A unique image is required — no placeholder reuse.
- **New JTU caption set: English, not Japanese.** 425's tables use `Pos / StartNumber / Name / Affiliation / YOB / Time / Swim / T1 / Bike / T2 / Run`, plus a **leading column whose `result_col_caption` is `null`** holding an internal athlete slug (`miyusakai`) — drop it. YOB was 0/81 populated. `Pos` carries DNF/DNS tokens, which is the normal convention: `parseRankAndStatus` reads status out of the overall_rank column, so no separate status column is needed. When writing the TSV prefer JTU-standard Japanese headers (総合順位/スイムラップ/総合記録…) over the verbatim English ones — tsv-lint's time and rank checks match the Japanese captions and `T1`/`T2`, but **not** `Time` or `Pos`, so verbatim English silently loses lint coverage.
- **Imported: IRONMAN 70.3 World Championship (Women) 2026-09-12 — Nice, France**, added to the existing `im703_wc_women` (3003 rows, 2689 finishers, 0 missing laps/totals). Venue is not in the labs-v2 payload at all; it came from the `/races/im703-world-championship-2026` page text ("Race Weekend September 12–13, 2026 … Nice, France", swim "de la Baie des Anges").
- **The 70.3 Worlds is one labs-v2 event split into per-gender subevents, and two slugs alias to it.** `im703-world-championship-2026` and `im703-world-championship-2027` both resolve to canonical event uuid `9798aa20-f278-e111-b16a-005056956277`, so a scan sees every subevent twice — **dedupe by `subevent_uuid`** or the diff double-reports. Repo ids are `im703_wc_women` / `im703_wc_men` (plus `im703_wc` for the pre-2017 single-race years); the Women's race is the day before the Men's.
- **`im703_wc_men` 2026-09-13 deferred**: the race was held (ironman.com already had a winner story dated 2026-09-13) but labs-v2 listed **no** Men's subevent yet. Expect it under the same event uuid next run — this is the normal "subevent appears a day or two later" pattern, not a missing race.
- **BUG FOUND AND FIXED in `scripts/fetch-ironman-results.js`: the Country column was athlete-entered free text.** It read `wtc_ContactId.address1_country`, which mixes ISO2, ISO3 and casing in the same race (`US`/`USA`/`deu`/`are`/`ARG`/`abw` — **224 distinct values** for 3003 athletes). `parseResidence` can't resolve most of that, so **751/3003 (25%) normalized to `residence: null`**. The same API response carries `_wtc_countryrepresentingid_value_formatted` — a controlled country name ("United States"), 104 distinct, 0 blanks. Switched to it (falling back to the old field): nulls dropped to **22 (0.7%)**, matching the 2025 sibling's 15/2623. The measurement that exposed it was comparing distinct-country counts against the prior year's TSV — 2025 editions came from an older pipeline and have clean full names, so **a year-over-year column-shape comparison is worth doing on any field, not just athlete counts.**
- **PENDING, deliberately not done: every other 2026 IRONMAN edition in the repo still carries the garbage country values** (im703_baku 132 distinct, im703_vichy 78, ironman_ottawa 63, im703_knokke_heist 63, …). Re-fetching them with the fixed script would recover ~25% of their residence data but rewrites ~100 committed TSVs — left as a user decision rather than folded into a gap-fill run.
- **Don't "fix" the empty Status column.** 2025 IRONMAN TSVs carry `FIN` for finishers; the current script emits `""`. These are equivalent downstream — `build-athlete.js`'s `STATUS_COLUMN_MAP` maps `FIN` to null precisely so status comes from the rank column either way. Cosmetic drift only.
- Source-side noise left as-is in WC Women 2026: 7 rows with `gender=M` and 12 blank inside F age divisions (straight from `wtc_ContactId.gendercode`); 18 athletes normalize to `OPEN` (the 99999 rank placeholder — guide/PC-ID entries).
- **"2026 Oregon Currents to Kicks" (2026-07-17, uuid `2e2c00af-fe74-49ff-acda-a39cc2dfa69a`) is an aquathlon side event, not a missing triathlon** — 190 rows, swim+run only, 0/190 with a bike time, held the Friday of the `im703_oregon` 70.3 weekend (which is imported, 2026-07-19). The aquathlon policy covers *standalone age-group championships*, so it's skipped. Don't re-flag it.
- **2025 was re-diffed in full (155 subevents): zero real gaps.** Every non-match was a slug alias whose repo id sits on the same date. Confirmed alias pairs, for future diffs: `im703-oceanside`→`im703_california`, `im-penghu`→`ironman_taiwan`, `im703-brasilia`→`im703_brazil`, `im703-venice-jesolo`→`im703_venice`, `im703-danang`→`ironman_vietnam`, `im703-aix-en-provence`→`im703_aix`, `im703-shanghai-chongming`→`im703_shanghai`, `im703-western-massachusetts`→`im703_western_mass`, `im-tours`/`im703-tours`→`ironman_tours_metropole_loire_valley`, `im703-goseong`→`im703_goseong_korea`, `im703-pennsylvania`→`im703_happy_valley`, `im703-rockford-illinois`→`im703_rockford`, `im703-les-sables-dolonne`→`im703_les_sables_dolonne_vendee`, `im703-luxembourg`→`im703_luxembourg_region_moselle`, `im703-ruidoso-new-mexico`→`im703_ruidoso`, `im-canada-ottawa`→`ironman_ottawa`, `im703-cebu-philippines`→`im703_lapu_lapu`, `im-kalmar`→`ironman_sweden`, `im703-dallas-little-elm`→`im703_dallas`. A `slug.replace(/-/g,'_')` plus an `im_`→`ironman_` rule resolves 133/169; the rest need the date cross-check, so **diff by date and confirm by name — don't treat an unresolved slug as a gap.**
- Event-level `location` / `location_parts` are meaningless for rotating-venue championships: `im703_wc_women` says `location: "Marbella, Spain"` with `location_parts` Kailua-Kona/Hawaii/US, and now hosts a Nice 2026 edition. Per-edition venue lives only in the category `description`. Pre-existing; flagged, not fixed.
- `im703_subic_bay_philippines` remains a stale duplicate of `im703_subic_bay` (its 2026-06-07 subevent resolves to the canonical id, which is imported). Third consecutive run noting it unfixed.
- IRONMAN sweep: 169/169 pages fetched with `__NEXT_DATA__` present and 0 redirect cases; 168 listed subevents. **`im703-versailles` listed none** — no results ever published, and the event is absent from race-info.json entirely, so it is not a gap. If a future run sees a subevent under it, treat it as a genuinely new event. Coverage caveat unchanged — the repo has 319 IRONMAN-family events vs 169 slugs in `scripts/ironman-site-event-uuids.json`, but the uncovered ones are by definition not on ironman.com's current sitemap, so they aren't racing now.
